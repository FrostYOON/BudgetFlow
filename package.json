{
  "name": "budgetflow",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.7.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "turbo run format",
    "db:format": "pnpm --filter @budgetflow/database prisma:format"
  },
  "devDependencies": {
    "turbo": "^2.0.12"
  }
}
