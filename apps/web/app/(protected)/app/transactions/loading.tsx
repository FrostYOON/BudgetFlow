import { AppLoadingState } from "@/components/feedback/app-loading-state";

export default function TransactionsLoading() {
  return <AppLoadingState bodyHeights={["h-16 rounded-[1.75rem]", "h-[36rem] rounded-[1.75rem]"]} />;
}
